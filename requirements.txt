# Python dependencies for the clonal-coupling pipeline.
#
#     python -m pip install -r requirements.txt
#
# Lower bounds reflect the APIs actually used. The pipeline was verified with
# Python 3.12.10, numpy 2.4.6, pandas 2.3.3, networkx 3.6.1, matplotlib 3.11.1
# and joblib 1.5.3.

numpy>=1.24
pandas>=2.0
networkx>=3.0
matplotlib>=3.7
joblib>=1.3

# Optional: only needed for the interactive HTML network written by step 2.
# The rest of the pipeline runs without it.
plotly>=5.0
